# Skill: Audit API Endpoints

1. Enumerate routes and classify public, authenticated, admin, webhook, and internal endpoints.
2. Check validation, authn, authz, rate limits, errors, and CORS per route.
3. Confirm direct API requests cannot bypass frontend controls.
4. Add or recommend focused abuse tests.
